import 'package:flutter/material.dart';

class TalkZoneScreen extends StatelessWidget {
  const TalkZoneScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Talk It Out Zone',
        style: TextStyle(fontSize: 22),
      ),
    );
  }
}
